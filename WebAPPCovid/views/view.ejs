<!DOCTYPE html>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="mt-3 d-flex justify-content-center">
        <div class="row">
            <div class="p-3 mb-2 bg-primary text-white text-center">
                <div>สถานะการ Covid-19 <br> ในประเทศไทย <br>
                    <%if(data.length > 0){%>
                        <script> 
                        var _new = '<%= data[data.length-1].date %>'
                        var currentDate = new Date(_new)                  
                        var day = currentDate.getDate().toString().padStart(2, "0");
                        var month = (currentDate.getMonth() + 1) .toString().padStart(2, "0");
                        var year = currentDate.getFullYear()                        
                        document.write("<p>วันที่ " + _new.toString().substring(8,10) +'/'+ _new.toString().substring(5,7) +'/'+ _new.toString().substring(0,4) + "</p>")
                        </script>                       
                    <%}else{xxxx}%>
                </div>                
            </div>
            <div class="p-3 mb-2 bg-warning text-dark text-center">
                <div class="p-3 border">ผู้ติดเชื้อใหม่ <br>                     
                    <%if(data.length > 0){%>
                        <script>
                            var _new = '<%- JSON.stringify(data[data.length-1].new_Amount) %>'
                            document.write("+"+ _new + " ราย") 
                        </script>
                    <%}else{%>
                        xxxx
                    <%}%>
                </div>                  
            </div>
            <div class="col-6 p-3 mb-2 bg-success text-white text-center">
                <div class="p-3 border">หายป่วย <br>
                    <%if(data.length > 0){%>
                        <script>                    
                            var _old = '<%- JSON.stringify(data[data.length-1].old_GetWell_Amount) %>'
                            document.write(_old + " ราย") 
                        </script>
                    <%}else{%>
                        xxxx
                    <%}%>
                </div>
            </div>
            <div class="col-6 p-3 mb-2 bg-success text-white text-center">
                <div class="p-3 border">หายป่วยสะสม <br> 
                    <%if(data.length > 0){%>
                        <script>                    
                            var sum_old = '<%- JSON.stringify(data[data.length-1].sumOld) %>'
                            document.write(sum_old + " ราย") 
                        </script>
                    <%}else{%>
                        xxxx
                    <%}%>
                </div>
            </div>
            <div class="col-6 p-3 mb-2 bg-danger text-white text-center">
                <div class="p-3 border">ผู้ป่วยสะสม <br>
                    <%if(data.length > 0){%>
                        <script>                    
                            var sum_new = '<%- JSON.stringify(data[data.length-1].sumNew) %>'
                            document.write(sum_new + " ราย") 
                        </script>
                    <%}else{%>
                        xxxx
                    <%}%>
                </div>
            </div>
            <div class="col-6 p-3 mb-2 bg-secondary text-white text-center">
                <div class="p-3 border">เสียชีวิต <br> 
                    <%if(data.length > 0){%>
                        <script>                    
                            var dieAmt = '<%- JSON.stringify(data[data.length-1].died_Amount) %>'
                            document.write(dieAmt + " ราย") 
                        </script>
                    <%}else{%>
                        xxxx
                    <%}%>
                </div>
            </div>
            <div class="col-6 p-3 mb-2">
                <div> <br> 
                    <a href="/">กลับ</a>
                </div>
            </div>
        </div>
      </div>
</body>
</html>